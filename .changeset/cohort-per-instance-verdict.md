---
'genie-react': patch
---

Answer `react_component_cohort` per instance instead of collapsing the whole result to `unknown`. Fibers the commit walk reached but the analysis budget declined are now recorded by identity, so a skipped fiber costs that one instance its verdict rather than every instance in the cohort. Instances the walk reached and did not record report `mounted-idle`, and an instance whose render was actually recorded keeps its `mounted-updated` verdict even when an unrelated coverage gap is open. Deferred lifecycle work remains unknown until it is processed, while a failed traversal keeps the rest of the observation window incomplete. Skipped-render identity detail is bounded and fails closed on overflow. `coverage.complete` keeps its existing meaning.
